{
  "name": "@claude-code-best/deepagents-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code, rebuilt on top of LangChain + deepagents — env detection, CLAUDE.md, plan mode, permissions, hooks, system reminders, and the missing tools (bash, web_fetch, web_search, ask_user_question, enter_plan_mode, exit_plan_mode).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tools": "./src/tools/index.ts",
    "./middleware": "./src/middleware/index.ts",
    "./settings": "./src/settings.ts",
    "./prompt": "./src/prompt.ts"
  },
  "bin": {
    "ccx": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.81.0",
    "@langchain/anthropic": "^0.3.0",
    "@langchain/core": "^0.3.0",
    "@langchain/langgraph": "^0.2.0",
    "deepagents": "^0.1.0",
    "langchain": "^0.3.0",
    "yaml": "^2.8.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
